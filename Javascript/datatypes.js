"use strict"
let str = 'Rohan';
//Backticks are extendent functionality quotes. 
//They allow us to embed variables and expression int a string by wrapping them in ${..}
let phrase = `Hello ${str}`;   
alert(phrase);
alert(`Result is ${2+4}`);